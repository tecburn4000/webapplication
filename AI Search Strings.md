
# FTP
## Transfer
```
spring boot 3 security thymeleaf FtpRemoteFileTemplate application.properties gradle kann ich eine datei von einem server übertragen ohne sie lokal zu speichern? Also user klickt im ftp browser auf datei und diese wird dann zu seinem recher übertragen?
```

### HTML-File-Links zur Navigation
```
spring boot 3 security thymeleaf FtpRemoteFileTemplate die dateien im html als klickbare links anzeigen?
```

### Browse FTP
```
spring boot 3 security thymeleaf FtpRemoteFileTemplate 
wie kann ich am besten durch die ftp directory browsen?
```

### Chat string
```
spring boot 3 security 6 thymeleaf FtpRemoteFileTemplateKombinierte SSR + JWT + Basic Auth wie generiere ich jwt tokens?
wie generiere ich einen jwt für den api/stream request?
spring boot 3 security thymeleaf FtpRemoteFileTemplate application.properties gradle 
```

### JWT
```
ich habe eine spring boot 3 security 6 gradle application.properties anwendung mit usermanagement (ssr) und benötige einen rest endpunkt der mit per ftp dateien von einem ftp server über FtpRemoteFileTemplate zur verfügung stellt. der rest endpunkt soll über basic auth und jwt abgesichert sein
```

### Generelle AI Beschreibung
```
spring boot 3 security 6 thymeleaf gradle application.properties webendpunkt und rest endpunkt securityconfig wenn ich zwei SecurityFilterChains eine für web eine für api kann man im web unangemeldet zugreifen
```